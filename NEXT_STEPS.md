# 🚀 مراحل بعدی - دقیقاً الان چه کار کنید؟
# What to do RIGHT NOW?

---

## ✅ وضعیت فعلی شما / Current Status

شما Backend را با موفقیت راه‌اندازی کرده‌اید:
- ✅ Backend نصب شده است
- ✅ دیتابیس SQLite آماده است
- ✅ سرور روی پورت 5000 در حال اجراست

---

## 🎯 همین الان 5 دقیقه وقت بگذارید و این‌ها را انجام دهید:

### ⏱️ دقیقه 1: باز کردن ترمینال جدید

باید **دو ترمینال** باز داشته باشید:
- ترمینال 1: Backend (که الان باز است)
- ترمینال 2: Frontend (باید باز کنید)

در PowerShell جدید:

```powershell
cd frontend
```

### ⏱️ دقیقه 2: نصب Frontend (اگر لازم است)

اگر قبلاً نصب نکرده‌اید:

```powershell
npm install
```

اگر نصب کرده‌اید، این مرحله را رد کنید.

### ⏱️ دقیقه 3: ایجاد فایل .env

```powershell
cp .env.example .env
```

### ⏱️ دقیقه 4: اجرای Frontend

```powershell
npm run dev
```

باید این را ببینید:
```
  VITE v5.1.0  ready in 300 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
  ➜  press h + enter to show help
```

### ⏱️ دقیقه 5: باز کردن مرورگر

مرورگر خود را باز کنید و به این آدرس بروید:

**http://localhost:5173**

---

## 👤 ایجاد کاربر اول (مهم!)

صفحه Login را می‌بینید، اما دیتابیس خالی است!

### دو روش دارید:

#### روش ۱: از طریق Frontend (ساده‌تر)

1. روی دکمه "ثبت‌نام" یا "Register" کلیک کنید
2. این اطلاعات را وارد کنید:

```json
ایمیل / Email: admin@royaljeans.com
رمز عبور / Password: 123456
نام کامل / Full Name: مدیر سیستم
نقش / Role: ADMIN
```

3. روی "ثبت‌نام" کلیک کنید
4. حالا با همان ایمیل و رمز عبور وارد شوید

#### روش ۲: با PowerShell (برای حرفه‌ای‌ها)

```powershell
curl -X POST http://localhost:5000/api/auth/register `
  -H "Content-Type: application/json" `
  -d '{
    "email": "admin@royaljeans.com",
    "password": "123456",
    "fullName": "مدیر سیستم",
    "role": "ADMIN"
  }'
```

---

## 🎉 تبریک! سیستم آماده است!

حالا می‌توانید:
- ✅ به داشبورد دسترسی داشته باشید
- ✅ سفارش ایجاد کنید
- ✅ پیمانکاران را مدیریت کنید
- ✅ گزارشات ببینید

---

## 📱 تست کردن سیستم

### 1. ایجاد یک سفارش نمونه

1. به منوی "سفارشات" بروید
2. روی "ایجاد سفارش" کلیک کنید
3. یک کد سفارش وارد کنید (مثلاً: `ORD-001`)
4. سایزبندی‌ها را پر کنید
5. ذخیره کنید

### 2. ثبت یک پیمانکار

1. به منوی "پیمانکاران" بروید
2. روی "افزودن پیمانکار" کلیک کنید
3. نام و نوع را وارد کنید
4. ذخیره کنید

### 3. مشاهده گزارشات

1. به منوی "گزارشات" بروید
2. فیلترها را تنظیم کنید
3. خروجی Excel را دانلود کنید

---

## 🛠️ اگر مشکلی پیش آمد

### مشکل: Frontend بالا نمی‌آید

```powershell
# در ترمینال Frontend:
cd frontend
rm -rf node_modules
rm package-lock.json
npm install
npm run dev
```

### مشکل: به Backend وصل نمی‌شود

```powershell
# در ترمینال Backend (ترمینال 1):
cd backend
npm run dev
```

مطمئن شوید هر دو ترمینال باز هستند و هر دو سرور در حال اجرا.

### مشکل: صفحه سفید است

1. مرورگر را Refresh کنید (F5)
2. Console مرورگر را باز کنید (F12)
3. خطاها را ببینید

---

## 📚 برای مطالعه بیشتر

### اگر می‌خواهید همه چیز را بدانید:

1. **راهنمای کامل فارسی** (`راهنمای_کامل.md`)
   - توضیحات کامل همه بخش‌ها
   - API Endpoints
   - ساختار دیتابیس

2. **راهنمای کامل انگلیسی** (`QUICK_GUIDE_EN.md`)
   - Everything in English
   - Detailed explanations

3. **راهنمای سریع** (`QUICK_START.md`)
   - شروع سریع
   - مثال‌ها

### برای توسعه:

- **CHAKRA_UI_SETUP.md** - یادگیری Chakra UI
- **STANDALONE_APP_PLAN.md** - پلن کامل پروژه
- **BUILD_PLAN.md** - پلن ساخت

---

## 🔧 دستورات مفید

### Backend
```bash
cd backend
npm run dev              # Start server
npm run prisma:studio    # View database GUI
```

### Frontend
```bash
cd frontend
npm run dev              # Start dev server
npm run build            # Build for production
```

---

## 💡 نکات مهم

1. **همیشه دو ترمینال باز داشته باشید**
   - یکی برای Backend
   - یکی برای Frontend

2. **هرگز ترمینال Backend را نبندید**
   - اگر بستید، مجدداً `npm run dev` را اجرا کنید

3. **اگر خطا دیدید، لاگ‌ها را بخوانید**
   - در ترمینال، خطاها با رنگ قرمز نمایش داده می‌شوند

4. **مرورگر را گاهی Refresh کنید**
   - تغییرات Frontend معمولاً خودکار است
   - اما گاهی نیاز به Refresh دارید

---

## 🎯 چک‌لیست نهایی

قبل از اینکه ادامه دهید، مطمئن شوید:

- [ ] Frontend نصب شده است
- [ ] فایل `.env` در frontend ایجاد شده
- [ ] Frontend در حال اجراست (http://localhost:5173)
- [ ] Backend در حال اجراست (http://localhost:5000)
- [ ] کاربر Admin ثبت‌نام شده
- [ ] می‌توانید به سیستم وارد شوید
- [ ] داشبورد را می‌بینید

---

## 🆘 کمک لازم دارید؟

اگر به مشکلی برخوردید که حل نشد:

1. **لاگ‌ها را بخوانید** - در ترمینال
2. **Console مرورگر را بررسی کنید** - با F12
3. **Health Check را تست کنید**:
   ```bash
   curl http://localhost:5000/health
   ```

---

## 🎉 آماده شروع کار هستید!

ساختار سیستم:

```
Backend (Port 5000)
    ↓ API
Frontend (Port 5173)
    ↓ User Interface
شما! (کاربر)
```

حالا می‌توانید:
- سفارشات را مدیریت کنید
- پیمانکاران را ثبت کنید
- گزارشات بگیرید
- آمار ببینید

---

**موفق باشید!** 🏭👕

ساخته شده برای **رویال جینز**
