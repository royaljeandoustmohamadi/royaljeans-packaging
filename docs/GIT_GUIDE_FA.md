# راهنمای جامع گیت - کار با VS Code و گیت‌هاب

این راهنما به شما کمک می‌کند تا بتوانید به بهترین شکل بین VS Code در سیستم خود و گیت‌هاب ارتباط برقرار کنید.

## فهرست مطالب

1. [مفاهیم پایه](#مفاهیم-پایه)
2. [تنظیمات اولیه](#تنظیمات-اولیه)
3. [چرخه کار گیت](#چرخه-کار-گیت)
4. [دستور Pull](#دستور-pull)
5. [دستور Push](#دستور-push)
6. [دستور Merge](#دستور-merge)
7. [کار با VS Code](#کار-با-vs-code)
8. [سناریوهای رایج](#سناریوهای-رایج)
9. [حل مشکلات](#حل-مشکلات)

---

## مفاهیم پایه

### Repository (ریپازیتوری)
یک پوشه که تمام فایل‌ها و تاریخچه تغییرات پروژه شما را نگه می‌دارد.

### Branch (برنچ)
یک نسخه مستقل از پروژه که می‌توانید روی آن کار کنید بدون اینکه روی نسخه اصلی تأثیر بگذارید.

- **main/master**: برنچ اصلی پروژه
- **feature/***: برنچ برای افزودن قابلیت جدید
- **fix/***: برنچ برای رفع باگ
- **hotfix/***: برنچ برای رفع مشکلات فوری

### Commit
ثبت تغییرات در تاریخچه گیت. هر commit یک عکس فوری از وضعیت پروژه در آن لحظه است.

### Remote
نسخه آنلاین ریپازیتوری که روی گیت‌هاب ذخیره شده است.

### Staging Area
محل موقت که فایل‌های آماده برای commit در آن قرار می‌گیرند.

---

## تنظیمات اولیه

### ۱. نصب گیت
از سایت [git-scm.com](https://git-scm.com/downloads) نصب کنید.

### ۲. پیکربندی نام و ایمیل
در ترمینال VS Code:

```bash
git config --global user.name "نام شما"
git config --global user.email "ایمیل شما"
```

### ۳. اتصال به گیت‌هاب

**روش اول: با SSH (پیشنهادی)**
```bash
# تولید کلید SSH
ssh-keygen -t ed25519 -C "ایمیل شما"

# کپی کردن کلید عمومی
cat ~/.ssh/id_ed25519.pub
```

سپس این کلید را در گیت‌هاب اضافه کنید:
- Settings → SSH and GPG keys → New SSH key

**روش دوم: با Personal Access Token**
در گیت‌هاب:
- Settings → Developer settings → Personal access tokens → Tokens (classic)
- توکن بسازید و آن را ذخیره کنید

هنگام push کردن، به جای پسورد، توکن را وارد کنید.

### ۴. کلون کردن پروژه
```bash
git clone git@github.com:username/repo-name.git
# یا
git clone https://github.com/username/repo-name.git
```

---

## چرخه کار گیت

```
Working Directory (پوشه کاری)
    ↓
Staging Area (git add)
    ↓
Local Repository (git commit)
    ↓
Remote Repository (git push)
```

---

## دستور Pull

### Pull چیست؟
دریافت آخرین تغییرات از گیت‌هاب و ادغام آن با کد محلی شما.

### استفاده پایه
```bash
git pull origin main
```

### گزینه‌های مهم

**1. Pull با استراتژی rebase (پیشنهادی)**
```bash
git pull --rebase origin main
```
این دستور تغییرات شما را روی تغییرات جدید قرار می‌دهد و commit history تمیزتر می‌شود.

**2. Pull فقط برای دیدن تغییرات بدون ادغام**
```bash
git fetch origin main
git log HEAD..origin/main
```

**3. Pull از یک برنچ دیگر**
```bash
git pull origin feature/new-feature
```

### چه زمانی از Pull استفاده کنیم؟

- ✅ قبل از شروع کار روی پروژه
- ✅ قبل از push کردن تغییرات
- ✅ وقتی دیگران روی همان برنچ کار می‌کنند
- ✅ بعد از هر تغییر در گیت‌هاب

---

## دستور Push

### Push چیست؟
ارسال تغییرات محلی شما به گیت‌هاب.

### استفاده پایه
```bash
git push origin main
```

### گزینه‌های مهم

**1. Push یک برنچ جدید**
```bash
git push -u origin feature/new-feature
```
فلگ `-u` برنچ را به گیت‌هاب لینک می‌کند.

**2. Push برای آپدیت کردن یک PR**
```bash
git push
```

**3. Push به برنچ دیگر**
```bash
git push origin feature-branch:main
```

**4. Push با force (با احتیاط استفاده کنید)**
```bash
git push --force-with-lease
```
فقط در موارد خاص و بعد از مشورت با تیم استفاده کنید.

### چه زمانی از Push استفاده کنیم؟

- ✅ بعد از کامل شدن یک قابلیت یا رفع باگ
- ✅ بعد از commit کردن تغییرات
- ✅ وقتی می‌خواهید تغییرات را با تیم به اشتراک بگذارید

---

## دستور Merge

### Merge چیست؟
ادغام تغییرات یک برنچ در برنچ دیگر.

### استفاده پایه
```bash
# ابتدا به برنچ مقصد بروید
git checkout main
# یا
git switch main

# سپس ادغام کنید
git merge feature/new-feature
```

### انواع Merge

**1. Fast-forward Merge (ساده‌ترین)**
```bash
git merge feature-branch
```
وقتی برنچ مقصد تغییر نکرده است.

**2. Three-way Merge**
```bash
git merge --no-ff feature-branch
```
همیشه یک merge commit می‌سازد، حتی اگر fast-forward باشد.

**3. Squash Merge (تمیز کردن تاریخچه)**
```bash
git merge --squash feature-branch
git commit -m "توضیحات کامل تغییرات"
```
تمام commit‌ها را در یک commit ادغام می‌کند.

### حل Conflict (تضاد)

وقتی دو نفر یک خط را تغییر دهند:

```bash
git merge feature-branch

# Conflict رخ می‌دهد:

<<<<<<< HEAD
// کد شما
const name = "Ali";
=======
// کد دیگران
const name = "Reza";
>>>>>>> feature-branch
```

**حل کردن Conflict:**

1. فایل‌های conflict را باز کنید
2. کد صحیح را انتخاب کنید و علامت‌های <<<<<<< و >>>>>>> را حذف کنید
3. فایل را ذخیره کنید

```bash
# اضافه کردن فایل‌های حل شده
git add filename.js

# تکمیل merge
git commit
```

### چه زمانی از Merge استفاده کنیم؟

- ✅ بعد از تایید یک Pull Request
- ✅ وقتی یک قابلیت کامل شد
- ✅ برای آوردن تغییرات از main به برنچ خودتان

---

## کار با VS Code

### منبع کنترل (Source Control)

1. **نمایش تغییرات:**
   - روی آیکون منبع کنترل کلیک کنید (Ctrl+Shift+G)
   - تغییرات نمایش داده می‌شود

2. **Stage کردن فایل‌ها:**
   - روی علامت + کنار هر فایل کلیک کنید
   - یا همه را با Change کنار نام فایل انتخاب کنید

3. **Commit کردن:**
   - در کادر پیام، توضیحات را بنویسید
   - روی علامت ✔ کلیک کنید یا Ctrl+Enter بزنید

### منبع گیت (Git Graph)

برای دیدن تاریخچه گرافیکی:
1. دکمه Ctrl+Shift+P را بزنید
2. "Git Graph" را جستجو کنید

### استفاده از ترمینال VS Code

ترمینال را با `Ctrl+~` باز کنید و دستورات گیت را اجرا کنید.

### شورت‌کات‌های مهم

| کلید | عمل |
|------|------|
| `Ctrl+Shift+G` | باز کردن منبع کنترل |
| `Alt+Click` | انتخاب چند خط |
| `Ctrl+Enter` | Commit سریع |
| `Ctrl+Shift+P` | پنل دستورات |

---

## سناریوهای رایج

### سناریو ۱: شروع کار روی یک قابلیت جدید

```bash
# 1. آپدیت کردن main
git checkout main
git pull origin main

# 2. ساخت برنچ جدید
git checkout -b feature/user-authentication
# یا
git switch -c feature/user-authentication

# 3. کار روی کد...
# ... کدنویسی ...

# 4. Stage و Commit
git add .
git commit -m "feat: add user authentication"

# 5. Push به گیت‌هاب
git push -u origin feature/user-authentication

# 6. ساخت PR در گیت‌هاب
```

### سناریو ۲: ادامه کار بعد از چند روز

```bash
# 1. آپدیت کردن برنچ خودتان
git checkout feature/user-authentication
git pull --rebase origin main

# 2. حل مشکلات如果有
# ...

# 3. ادامه کار و commit
git add .
git commit -m "fix: resolve authentication issue"

# 4. Push
git push
```

### سناریو ۳: رفع باگ سریع (Hotfix)

```bash
# 1. از main برنچ بسازید
git checkout main
git pull origin main
git checkout -b hotfix/critical-bug

# 2. رفع باگ
# ...

# 3. Commit و push
git add .
git commit -m "hotfix: fix critical login bug"
git push -u origin hotfix/critical-bug

# 4. PR و merge سریع
```

### سناریو ۴: کار تیمی روی یک برنچ

```bash
# قبل از شروع کار
git checkout shared-branch
git pull --rebase origin shared-branch

# کار روی کد...
git add .
git commit -m "your changes"

# قبل از push
git pull --rebase origin shared-branch

# اگر conflict دارد، حل کنید
# ...

# push
git push
```

---

## حل مشکلات

### مشکل ۱: Push رد می‌شود

```
error: failed to push some refs to ...
```

**راه‌حل:**
```bash
git pull --rebase origin main
git push
```

### مشکل ۲: فایل‌های ناخواسته در تغییرات

**راه‌حل:**
```bash
# unstaging
git reset HEAD filename

# حذف از گیت (اما نگه داشتن در سیستم)
git rm --cached filename
```

### مشکل ۳: اشتباه در آخرین commit

**راه‌حل:**
```bash
# تغییر پیام آخرین commit
git commit --amend -m "پیام صحیح"

# اضافه کردن فایل فراموش شده
git add forgotten-file.js
git commit --amend --no-edit
```

### مشکل ۴: تغییرات بهم ریخته شده

**راه‌حل:**
```bash
# بازگشت به آخرین commit معروف
git reset --hard HEAD

# یا بازگشت به 3 commit قبل
git reset --hard HEAD~3
```

### مشکل ۵: دیدن تغییرات commit‌ها

**راه‌حل:**
```bash
# نمایش آخرین 5 commit
git log -5 --oneline

# نمایش تغییرات یک commit
git show <commit-hash>

# مقایسه دو commit
git diff <commit1> <commit2>
```

---

## نکات مهم

### 1. Commit Messageهای خوب

```bash
# ❌ بد
git commit -m "fixed"

# ✅ خوب
git commit -m "fix: resolve login timeout issue"

# ✅ عالی
git commit -m "fix(auth): resolve login timeout for slow networks

- Increased timeout from 5s to 15s
- Added retry logic for failed requests
- Improved error messaging"
```

### 2. فرکانس Push

- هر 1-2 ساعت push کنید
- قبل از خروج از سیستم push کنید
- بعد از هر تغییر مهم push کنید

### 3. قبل از Merge

- حتماً تست کنید
- review تغییرات را ببینید
- از تیم بپرسید که کسی در حال push نیست

### 4. استفاده از Git Hooks

برای خودکار کردن فرآیندها:
```bash
# در پوشه .git/hooks
# فایل pre-commit بسازید و اسکریپت‌ها را قرار دهید
```

---

## واژه‌نامه فارسی-انگلیسی

| فارسی | انگلیسی |
|-------|---------|
| ادغام | Merge |
| دریافت | Pull |
| ارسال | Push |
| تعارض | Conflict |
| شاخه | Branch |
| تایید | Commit |
| مرحله‌بندی | Stage |
| مخزن | Repository |
| از راه دور | Remote |
| فوروارد سریع | Fast-forward |

---

## منابع بیشتر

- [Pro Git Book](https://git-scm.com/book/fa/v2)
- [GitHub Learning Lab](https://lab.github.com/)
- [VS Code Git Documentation](https://code.visualstudio.com/docs/sourcecontrol/overview)

---

## سوالات متداول

### سوال: آیا باید هر روز pull کنم؟
**جواب:** بله! حداقل یک بار در روز، و حتماً قبل از شروع کار جدید.

### سوال: اگر push رد شد چه کنم؟
**جواب:** اول pull کنید، اگر conflict داشت حل کنید، دوباره push کنید.

### سوال: آیا می‌توان commit را حذف کرد؟
**جواب:** بله، اما با احتیاط! `git reset` استفاده کنید.

### سوال: چند برنچ همزمان باز نگه دارم؟
**جواب:** معمولاً 1-2 برنچ کافی است. بقیه را بعد از merge حذف کنید.

---

**موفق باشید! 🚀**

این راهنما را به عنوان مرجع نگه دارید و با تمرین، به مرور بهتر می‌شوید.
