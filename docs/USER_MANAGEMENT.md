# سیستم مدیریت کاربران و پروفایل - Royal Jeans

این مستند شامل توضیحات کامل امکانات جدید سیستم مدیریت کاربران و پروفایل است.

## 🚀 ویژگی‌های جدید

### 1. سیستم پروفایل کاربری پیشرفته

#### ویژگی‌های اصلی:
- **دو نوع نام**: 
  - `نام اصلی` (فقط ادمین می‌تواند تغییر دهد)
  - `نام مستعار` (کاربر می‌تواند تغییر دهد)
- **تصویر پروفایل**: آپلود و مدیریت عکس کاربر
- **اطلاعات شخصی**: شماره تماس، توضیحات
- **ایمیل غیرقابل تغییر**: امنیت بالا

#### صفحه پروفایل:
- آدرس: `/profile`
- امکانات:
  - مشاهده و ویرایش اطلاعات شخصی
  - تغییر رمز عبور با امنیت بالا
  - آپلود تصویر پروفایل
  - پیش‌نمایش تغییرات

### 2. سیستم مدیریت کاربران (مخصوص ادمین)

#### ویژگی‌ها:
- **مشاهده لیست کاربران**: تمام اطلاعات کاربران
- **فیلترهای پیشرفته**: 
  - جستجو در نام، ایمیل، نام مستعار
  - فیلتر بر اساس نقش (ادمین، مدیر، کاربر)
  - فیلتر بر اساس وضعیت (فعال/غیرفعال)
- **عملیات کاربران**:
  - مشاهده جزئیات کامل
  - ویرایش اطلاعات و نقش‌ها
  - فعال/غیرفعال کردن حساب
  - حذف کاربر (با تأیید)

#### آدرس: `/admin/users`

### 3. UI Components مدرن

#### StatCard
```jsx
import { StatCard } from '../components/ui';

<StatCard
  title="سفارشات امروز"
  value="25"
  change="+12%"
  changeType="increase"
  icon={ShoppingCart}
  colorScheme="blue"
/>
```

#### DataTable
```jsx
import { DataTable } from '../components/ui';

const columns = [
  { key: 'name', header: 'نام', align: 'right' },
  { key: 'email', header: 'ایمیل', align: 'right' },
  { key: 'role', header: 'نقش', align: 'center' },
];

<DataTable
  columns={columns}
  data={users}
  onRowClick={handleRowClick}
  selectable={true}
  selectedRows={selectedIds}
  onSelectRow={handleSelectRow}
  onSelectAll={handleSelectAll}
  pagination={pagination}
/>
```

#### Notification System
```jsx
import { showSuccess, showError, showWarning } from '../components/ui';

showSuccess(toast, 'عملیات با موفقیت انجام شد');
showError(toast, 'خطا در انجام عملیات');
showWarning(toast, 'توجه: این عملیات قابل بازگشت نیست');
```

### 4. بهبودهای Backend

#### API Endpoints جدید:

**آپلود تصویر پروفایل:**
```
POST /api/users/avatar
Content-Type: multipart/form-data
```

**به‌روزرسانی پروفایل کاربر:**
```
PUT /api/users/:id
{
  "nickname": "نام جدید",
  "phone": "شماره تماس",
  "bio": "توضیحات",
  "avatar": "آدرس تصویر"
}
```

**دریافت لیست کاربران (ادمین):**
```
GET /api/users
Response: { users: [...] }
```

#### امنیت:
- رمز عبور با bcrypt hash
- JWT برای احراز هویت
- کنترل دسترسی بر اساس نقش
- محدودیت آپلود فایل (5MB)

## 🛠 نصب و راه‌اندازی

### 1. پایگاه داده

```bash
cd backend
npx prisma generate
npx prisma migrate dev --name add_user_profile_fields
```

### 2. نصب dependencies

```bash
# Backend
cd backend
npm install multer helmet compression cors

# Frontend
cd frontend
npm install
```

### 3. ساختار فایل‌ها

```
backend/
├── src/
│   ├── routes/
│   │   ├── users.js      # مدیریت کاربران
│   │   ├── avatars.js    # آپلود تصویر
│   │   └── auth.js       # احراز هویت
│   └── index.js          # تنظیمات اصلی
├── uploads/              # فایل‌های آپلود شده
└── prisma/
    ├── schema.prisma     # مدل داده
    └── migrations/       # مهاجرت‌ها

frontend/
├── src/
│   ├── components/ui/    # کامپوننت‌های مدرن
│   │   ├── StatCard.jsx
│   │   ├── DataTable.jsx
│   │   └── Notification.jsx
│   ├── pages/
│   │   ├── profile/      # صفحات پروفایل
│   │   └── admin/        # صفحات مدیریت
│   └── services/
│       ├── user.service.js
│       └── auth.service.js
```

## 🔧 راهنمای استفاده

### برای کاربران عادی:

1. **ورود به سیستم**: `/login`
2. **مشاهده پروفایل**: کلیک روی آواتار → پروفایل
3. **ویرایش اطلاعات**:
   - تغییر نام مستعار
   - آپلود تصویر
   - ویرایش شماره تماس و توضیحات
4. **تغییر رمز عبور**: تب "تغییر رمز عبور"

### برای ادمین:

1. **دسترسی به مدیریت**: منوی کناری → مدیریت
2. **مدیریت کاربران**:
   - جستجو و فیلتر کاربران
   - مشاهده جزئیات (کلیک روی آیکون چشم)
   - ویرایش (کلیک روی آیکون مداد)
   - حذف (کلیک روی آیکون سطل زباله)
3. **تنظیمات سیستم**: مدیریت لیست‌های انتخابی

## 🔐 نقش‌های کاربری

### 1. ADMIN (مدیر سیستم)
- دسترسی کامل به همه امکانات
- مدیریت کاربران (ویرایش، حذف، تغییر نقش)
- دسترسی به صفحه مدیریت
- تغییر نام اصلی کاربران

### 2. MANAGER (مدیر محصول)
- مشاهده لیست کاربران
- مشاهده جزئیات کاربران
- دسترسی به گزارشات
- ثبت سفارشات و پیمانکاران

### 3. USER (کاربر عادی)
- مدیریت پروفایل شخصی
- تغییر نام مستعار
- آپلود تصویر
- تغییر رمز عبور
- ثبت و مشاهده سفارشات

## 🛡 امنیت

### 1. احراز هویت
- JWT token برای تمام درخواست‌ها
- مدت اعتبار token: 7 روز
- ذخیره امن در localStorage

### 2. کنترل دسترسی
- Middleware برای بررسی نقش
- محدودیت عملیات بر اساس نقش
- بررسی مالکیت داده

### 3. آپلود فایل
- محدودیت اندازه: 5MB
- فقط فرمت‌های تصویر: jpeg, jpg, png, gif, webp
- نام‌گذاری منحصر به فرد
- حذف فایل قدیمی

## 📊 کامپوننت‌های UI

### StatCard
```jsx
<StatCard
  title="آمار امروز"
  value="150"
  change="+8%"
  changeType="increase"
  icon={TrendingUp}
  colorScheme="green"
/>
```

### DataTable
```jsx
<DataTable
  columns={columns}
  data={data}
  selectable={true}
  pagination={{page: 1, pageSize: 10, total: 100}}
  actions={true}
/>
```

### Notification
```jsx
// موفق
showSuccess(toast, 'عملیات انجام شد');

// خطا
showError(toast, 'خطا رخ داده');

// هشدار
showWarning(toast, 'توجه');

// اطلاعات
showInfo(toast, 'اطلاعات');
```

## 🚀 قابلیت‌های آینده

### برنامه‌ریزی شده:
1. **سیستم اعلان‌ها**: اعلان‌های real-time
2. **گزارشات پیشرفته**: نمودارها و آمار
3. **سیستم پیام‌رسانی**: چت داخلی
4. **مدیریت دسترسی**: سطح‌بندی دقیق‌تر
5. **Backup خودکار**: پشتیبان‌گیری داده‌ها

## 📞 پشتیبانی

برای گزارش مشکل یا درخواست ویژگی جدید:
- ایمیل: support@royaljeans.com
- سیستم تیکت داخلی
- مستندات فنی: `/docs`

---

**توسعه داده شده برای Royal Jeans** 🏭