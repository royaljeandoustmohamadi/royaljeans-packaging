# 🏭 راهنمای کامل راه‌اندازی سیستم رویال جینز
## Complete Setup Guide for Royal Jeans Order Management System

---

## ✅ کاری که انجام داده‌اید (What You've Done)

شما با موفقیت بخش **Backend** را راه‌اندازی کرده‌اید:
- ✅ فایل `.env` را ایجاد کرده‌اید
- ✅ دیتابیس SQLite را راه‌اندازی کرده‌اید
- ✅ Migrationها را اجرا کرده‌اید
- ✅ سرور Backend را روی پورت 5000 اجرا کرده‌اید

---

## 📋 مرحله بعدی: راه‌اندازی Frontend

### ۱. به پوشه frontend بروید

```bash
cd frontend
```

### ۲. نصب وابستگی‌ها

اگر قبلاً نصب نکرده‌اید:

```powershell
npm install
```

اگر قبلاً نصب کرده‌اید، این مرحله را رد کنید.

### ۳. ایجاد فایل `.env`

```powershell
cp .env.example .env
```

### ۴. بررسی محتوای فایل `.env`

مطمئن شوید محتوای فایل به این صورت است:

```env
VITE_API_URL=http://localhost:5000/api
```

### ۵. اجرای سرور Frontend

**بسیار مهم: در یک ترمینال جدید (ترمینال جداگانه) اجرا کنید!**

```powershell
npm run dev
```

سرور Frontend روی `http://localhost:5173` اجرا می‌شود.

---

## 🌐 دسترسی به سیستم

### سرور Backend
- آدرس: `http://localhost:5000`
- Health Check: `http://localhost:5000/health`
- API Endpoints: `http://localhost:5000/api/*`

### سرور Frontend
- آدرس: `http://localhost:5173`
- مرورگر را باز کنید و به این آدرس بروید

---

## 👤 ورود به سیستم

### ثبت‌نام کاربر اول (Admin)

از آنجا که دیتابیس خالی است، باید اول کاربر را ثبت‌نام کنید:

**روش ۱: از طریق Postman یا مشابه آن**

```http
POST http://localhost:5000/api/auth/register

Content-Type: application/json

{
  "email": "admin@royaljeans.com",
  "password": "123456",
  "fullName": "مدیر سیستم",
  "role": "ADMIN"
}
```

**روش ۲: از طریق فرم ثبت‌نام در Frontend**

1. به `http://localhost:5173` بروید
2. روی دکمه "ثبت‌نام" کلیک کنید
3. اطلاعات زیر را وارد کنید:
   - ایمیل: `admin@royaljeans.com`
   - رمز عبور: `123456`
   - نام کامل: `مدیر سیستم`
   - نقش: ADMIN

### ورود به سیستم

پس از ثبت‌نام:

1. به صفحه ورود بروید
2. ایمیل: `admin@royaljeans.com`
3. رمز عبور: `123456`

---

## 🎯 بخش‌های اصلی سیستم

### ۱. داشبورد (Dashboard)
- مشاهده آمار کلی سفارشات
- نمایش گراف‌ها و نمودارها
- دسترسی سریع به بخش‌های مختلف

### ۲. مدیریت سفارشات (Orders)
- **لیست سفارشات**: مشاهده همه سفارشات با جستجو و فیلتر
- **ایجاد سفارش**: فرم کامل با 84 فیلد در 5 تب جداگانه
  - تب 1: اطلاعات اصلی (کد، تاریخ، وضعیت)
  - تب 2: سایزبندی (سالم، اقتصادی 1-3، نمونه، استوک)
  - تب 3: موجودی (ملزومات، دوخت، پارچه)
  - تب 4: اطلاعات تکمیلی
  - تب 5: توضیحات
- **ویرایش سفارش**: تغییر اطلاعات سفارش
- **حذف سفارش**: حذف سفارش (فقط برای Admin)

### ۳. مدیریت پیمانکاران (Contractors)
- **لیست پیمانکاران**: مشاهده همه پیمانکاران
- **ایجاد پیمانکار**: ثبت پیمانکار جدید با نوع:
  - پارچه (Fabric)
  - تولید (Production)
  - بسته‌بندی (Packaging)
  - سنگ‌شویی (Stone Washing)
- **ارزیابی پیمانکاران**: ثبت امتیاز و نظر
- **مشاهده تاریخچه ارزیابی‌ها**

### ۴. گزارشات (Reports)
- خروجی Excel از سفارشات
- فیلتر بر اساس تاریخ و وضعیت
- آمار و تحلیل‌ها

---

## 🔧 ساختار کامل پروژه

```
royaljeans-packaging/
├── backend/                      # Backend API (Node.js + Express)
│   ├── src/
│   │   ├── index.js            # Entry point سرور
│   │   ├── routes/             # API Routes
│   │   │   ├── auth.js         # Authentication endpoints
│   │   │   ├── orders.js       # Orders CRUD
│   │   │   ├── users.js        # User management
│   │   │   ├── contractors.js  # Contractors CRUD
│   │   │   └── reports.js      # Reports endpoints
│   │   ├── middleware/         # Middleware
│   │   │   └── auth.js         # JWT authentication
│   │   └── utils/              # Utility functions
│   ├── prisma/
│   │   ├── schema.prisma       # Database schema
│   │   └── dev.db              # SQLite database file
│   ├── .env                    # Environment variables
│   └── package.json
│
├── frontend/                     # Frontend App (React + Chakra UI)
│   ├── src/
│   │   ├── main.jsx            # React entry point
│   │   ├── App.jsx             # Main app component
│   │   ├── pages/              # Page components
│   │   │   ├── LoginPage.jsx   # Login page
│   │   │   ├── RegisterPage.jsx # Register page
│   │   │   ├── Dashboard.jsx   # Dashboard page
│   │   │   ├── OrdersList.jsx  # Orders list
│   │   │   ├── OrderCreate.jsx # Create order
│   │   │   ├── OrderDetail.jsx # Order details
│   │   │   └── Contractors.jsx # Contractors page
│   │   ├── components/         # Reusable components
│   │   ├── services/           # API services (axios)
│   │   │   └── api.js          # API client
│   │   ├── store/              # State management
│   │   │   └── authStore.js    # Auth state (Zustand)
│   │   └── theme/              # Chakra UI theme
│   ├── .env                    # Environment variables
│   └── package.json
│
└── راهنمای_کامل.md             # This file
```

---

## 🚀 دستورات مهم

### Backend

```bash
cd backend

# اجرای سرور در حالت توسعه
npm run dev

# اجرای سرور در حالت production
npm start

# مشاهده دیتابیس با Prisma Studio
npm run prisma:studio

# اجرای migration جدید
npm run prisma:migrate

# بازسازی Prisma Client
npm run prisma:generate
```

### Frontend

```bash
cd frontend

# اجرای سرور توسعه
npm run dev

# ساخت نسخه production
npm run build

# پیش‌نمایش نسخه build شده
npm run preview

# بررسی کد با ESLint
npm run lint
```

---

## 🗄️ دیتابیس

### جداول اصلی

1. **User** - کاربران سیستم
   - id, email, password, fullName, role
   - Roleها: ADMIN, MANAGER, USER

2. **Order** - سفارشات (84 فیلد)
   - اطلاعات اصلی: code, date, status, createdBy
   - سایزبندی: healthy, eco1, eco2, eco3, sample, stock
   - موجودی: requirements, sewing, fabric
   - و اطلاعات دیگر...

3. **Contractor** - پیمانکاران
   - id, name, type, contactInfo, isActive
   - Typeها: FABRIC, PRODUCTION, PACKAGING, STONE_WASHING

4. **ContractorEvaluation** - ارزیابی پیمانکاران
   - id, contractorId, rating, comment, evaluatedBy, createdAt

5. **AuditLog** - لاگ تغییرات
   - id, entity, entityId, action, userId, changes, createdAt

### مشاهده دیتابیس

```bash
cd backend
npm run prisma:studio
```

این یک UI گرافیکی برای مشاهده و ویرایش دیتابیس باز می‌کند.

---

## 🔐 API Endpoints

### Authentication

```http
# ثبت‌نام
POST /api/auth/register
Body: { email, password, fullName, role }

# ورود
POST /api/auth/login
Body: { email, password }

# دریافت پروفایل
GET /api/auth/profile
Headers: { Authorization: Bearer <token> }
```

### Orders

```http
# لیست همه سفارشات
GET /api/orders
Headers: { Authorization: Bearer <token> }

# ایجاد سفارش جدید
POST /api/orders
Headers: { Authorization: Bearer <token> }
Body: { ...orderData }

# دریافت یک سفارش
GET /api/orders/:id
Headers: { Authorization: Bearer <token> }

# ویرایش سفارش
PUT /api/orders/:id
Headers: { Authorization: Bearer <token> }
Body: { ...orderData }

# حذف سفارش
DELETE /api/orders/:id
Headers: { Authorization: Bearer <token> }
```

### Contractors

```http
# لیست همه پیمانکاران
GET /api/contractors
Headers: { Authorization: Bearer <token> }

# ایجاد پیمانکار جدید
POST /api/contractors
Headers: { Authorization: Bearer <token> }
Body: { name, type, contactInfo }

# دریافت یک پیمانکار
GET /api/contractors/:id
Headers: { Authorization: Bearer <token> }

# ویرایش پیمانکار
PUT /api/contractors/:id
Headers: { Authorization: Bearer <token> }
Body: { ...contractorData }

# حذف پیمانکار
DELETE /api/contractors/:id
Headers: { Authorization: Bearer <token> }

# ثبت ارزیابی
POST /api/contractors/:id/evaluations
Headers: { Authorization: Bearer <token> }
Body: { rating, comment }
```

### Reports

```http
# دریافت گزارش سفارشات
GET /api/reports/orders
Headers: { Authorization: Bearer <token> }
Query: ?startDate=&endDate=&status=

# خروجی Excel
GET /api/reports/orders/export
Headers: { Authorization: Bearer <token> }
Query: ?startDate=&endDate=&status=
Response: Excel file download
```

---

## 🐛 مشکلات رایج و حل آنها

### مشکل ۱: Frontend به Backend وصل نمی‌شود

**علت**: Backend در حال اجرا نیست یا پورت اشتباه است

**حل**:
```bash
# بررسی وضعیت Backend
# در ترمینال Backend بمانید و خروجی را ببینید

# یا Health Check را تست کنید
curl http://localhost:5000/health
```

### مشکل ۲: خطای CORS

**علت**: تنظیمات CORS صحیح نیست

**حل**:
1. فایل `backend/.env` را بررسی کنید
2. مطمئن شوید `NODE_ENV=development` تنظیم شده
3. Backend را مجدداً اجرا کنید

### مشکل ۳: دیتابیس کار نمی‌کند

**علت**: دیتابیس خراب شده یا migration اجرا نشده

**حل**:
```bash
cd backend

# حذف دیتابیس فعلی
rm prisma/dev.db

# اجرای مجدد migration
npx prisma migrate dev

# اجرای مجدد سرور
npm run dev
```

### مشکل ۴: خطای "Port already in use"

**علت**: پورت 5000 یا 5173 قبلاً در حال استفاده است

**حل**:
```bash
# پیدا کردن پروسه که از پورت استفاده می‌کند
netstat -ano | findstr :5000

# در Windows:
taskkill /PID <PID> /F

# یا پورت را در .env تغییر دهید
```

### مشکل ۵: خطای Authentication

**علت**: Token منقضی شده یا وجود ندارد

**حل**:
1. از سیستم خارج شوید (Logout)
2. مجدداً وارد شوید (Login)
3. اگر مشکل ادامه داشت، مرورگر را Refresh کنید

---

## 📱 تست کردن با Postman یا Thunder Client

### ۱. Health Check

```http
GET http://localhost:5000/health
```

پاسخ مورد انتظار:
```json
{
  "status": "OK",
  "timestamp": "2024-02-19T...",
  "version": "1.0.0"
}
```

### ۲. ثبت‌نام کاربر

```http
POST http://localhost:5000/api/auth/register
Content-Type: application/json

{
  "email": "test@royaljeans.com",
  "password": "123456",
  "fullName": "کاربر تست",
  "role": "USER"
}
```

### ۳. ورود

```http
POST http://localhost:5000/api/auth/login
Content-Type: application/json

{
  "email": "test@royaljeans.com",
  "password": "123456"
}
```

پاسخ:
```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": 1,
    "email": "test@royaljeans.com",
    "fullName": "کاربر تست",
    "role": "USER"
  }
}
```

### ۴. دریافت لیست سفارشات

```http
GET http://localhost:5000/api/orders
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

---

## 🎨 ویژگی‌های UI

### پشتیبانی از RTL (راست به چپ)
- تمام متون فارسی به درستی نمایش داده می‌شوند
- چیدمان صفحات راست به چپ است

### فونت فارسی
- از فونت Vazirmatn استفاده شده
- خوانایی بالا و زیبایی

### Dark Mode
- قابلیت تغییر بین حالت روشن و تاریک
- ذخیره تنظیمات در مرورگر

### ریسپانسیو
- نمایش صحیح در موبایل، تبلت و دسکتاپ
- منوی کناری که در موبایل مخفی می‌شود

---

## 🔒 امنیت

### نکات مهم برای Production

1. **تغییر JWT_SECRET**
   ```env
   # در فایل backend/.env
   JWT_SECRET="your-very-secure-secret-key-here"
   ```

2. **استفاده از دیتابیس قوی‌تر**
   - در production از PostgreSQL یا MySQL استفاده کنید
   - تنظیم در `prisma/schema.prisma`

3. **HTTPS**
   - در production حتماً از HTTPS استفاده کنید
   - تنظیم SSL Certificate

4. **Environment Variables**
   - هیچ وقت فایل `.env` را به git نفرستید
   - از `.env.example` استفاده کنید

---

## 📚 منابع تکمیلی

### مستندات موجود در پروژه

- `README.md` - توضیحات کلی پروژه
- `QUICK_START.md` - راهنمای سریع
- `CHAKRA_UI_SETUP.md` - راهنمای کامل Chakra UI
- `CHAKRA_UI_EXAMPLES.md` - مثال‌های UI
- `CHAKRA_UI_QUICKSTART.md` - شروع سریع Chakra UI
- `RTL_COMPONENTS_EXAMPLES.md` - مثال‌های کامپوننت‌های RTL
- `RTL_JALALI_GUIDE.md` - راهنمای تقویم شمسی
- `STANDALONE_APP_PLAN.md` - پلن کامل پروژه
- `BUILD_PLAN.md` - پلن ساخت

### لینک‌های مفید

- [Prisma Documentation](https://www.prisma.io/docs)
- [Express.js Documentation](https://expressjs.com/)
- [React Documentation](https://react.dev/)
- [Chakra UI Documentation](https://chakra-ui.com/)
- [Vite Documentation](https://vitejs.dev/)

---

## 🎯 چک‌لیست راه‌اندازی کامل

### Backend ✅
- [x] نصب وابستگی‌ها (`npm install`)
- [x] ایجاد فایل `.env`
- [x] اجرای migration (`npx prisma migrate dev`)
- [x] اجرای سرور (`npm run dev`)
- [ ] تست Health Check
- [ ] ثبت‌نام کاربر Admin

### Frontend
- [ ] نصب وابستگی‌ها (`npm install`)
- [ ] ایجاد فایل `.env`
- [ ] اجرای سرور (`npm run dev`)
- [ ] باز کردن مرورگر در `http://localhost:5173`
- [ ] ثبت‌نام کاربر
- [ ] ورود به سیستم
- [ ] تست ایجاد سفارش
- [ ] تست مدیریت پیمانکاران

---

## 💡 نکات توسعه

### افزودن فیلد جدید به سفارش

1. ویرایش `backend/prisma/schema.prisma`
2. اجرای `npx prisma migrate dev`
3. ویرایش `backend/src/routes/orders.js`
4. ویرایش `frontend/src/pages/OrderCreate.jsx`

### افزودن API Endpoint جدید

1. ایجاد فایل route در `backend/src/routes/`
2. اضافه کردن به `backend/src/index.js`
3. ایجاد service در `frontend/src/services/api.js`
4. استفاده در کامپوننت‌های React

### تغییرات UI

1. ویرایش کامپوننت‌ها در `frontend/src/components/`
2. ویرایش صفحات در `frontend/src/pages/`
3. تغییر تم در `frontend/src/theme/`

---

## 🆘 پشتیبانی

اگر به مشکلی برخوردید:

1. **خواندن لاگ‌ها**: خروجی ترمینال را بررسی کنید
2. **Health Check**: `http://localhost:5000/health` را تست کنید
3. **Prisma Studio**: برای مشاهده دیتابیس استفاده کنید
4. **Browser Console**: برای خطاهای Frontend

---

## 🎉 تبریک!

شما اکنون یک سیستم مدیریت سفارشات کامل دارید!
ساخته شده برای **رویال جینز** 🏭

---

**آخرین بروزرسانی**: فوریه 2024
**نسخه**: 1.0.0
